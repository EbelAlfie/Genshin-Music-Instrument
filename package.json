{
  "name": "genshin-music-project",
  "version": "1.0.0",
  "description": "Trying to duplicate genshin impact lyre playing",
  "scripts": {
    "this": "npx parcel index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-reporter-static-files-copy": "^1.5.0"
  },
  "dependencies": {
    "@parcel/config-default": "^2.8.3",
    "gh-pages": "^5.0.0",
    "three":"0.157.0"
  },
  "staticFiles": {
    "staticOutpath": "Assets"
  }
}
